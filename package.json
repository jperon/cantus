{
  "name": "musica",
  "version": "1.0.0",
  "description": "Liseuse MusicXML PWA — lecteur de partitions pour liseuse Android",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "build-spa": "node build.js --spa",
    "watch": "node build.js --watch",
    "serve": "npx http-server dist -p 8443 --ssl --cert localhost.pem --key localhost-key.pem -c-1"
  },
  "devDependencies": {
    "chokidar": "^3.6.0",
    "coffeescript": "^2.7.0",
    "pug": "^3.0.3",
    "verovio": "^4.4.0"
  },
  "dependencies": {
    "fflate": "^0.8.2"
  }
}
